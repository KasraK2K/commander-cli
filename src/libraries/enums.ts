export enum FileNames {
    ROUTES = 'routes',
    CONTROLLER = 'controller',
    SCHEMA = 'schema',
    SERVICE = 'service',
    REPOSITORY = 'repository',
    MODULE = 'module',
    REST = 'rest'
}

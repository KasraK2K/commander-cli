export enum FileNames {
    ROUTES = 'routes',
    CONTROLLER = 'controller',
    SCHEMA = 'schema',
    SERVICE = 'service',
    REPOSITORY = 'repository',
    QUERY = 'query',
    MODULE = 'module',
    REST = 'rest',
}

export enum LogTypes {
    VERBOSE,
    SUCCESS,
    WARNING,
    DEBUG,
    ERROR,
}
